defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      animation: false
      layout: default
sass:
  sass_dir: _scss
  style: compressed
url: "https://urtc.mit.edu"
